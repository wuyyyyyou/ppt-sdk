{
  "name": "anna-frontend-llm-agent-debug-app",
  "private": true,
  "version": "0.1.0",
  "description": "Anna app for debugging iframe-side LLM and agent runtime RPC behavior.",
  "scripts": {
    "dev": "anna-app dev",
    "validate": "anna-app validate --strict"
  },
  "devDependencies": {
    "@anna-ai/cli": "latest"
  }
}
