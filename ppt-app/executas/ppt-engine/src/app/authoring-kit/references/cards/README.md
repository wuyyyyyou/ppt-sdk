# Cards（卡片参考）

- `NumberedAgendaCard.tsx`：目录页需要“编号 + 标题 + 可选图标”时参考。
- `NarrativeListItem.tsx`：连续表达 2–4 条解释、观察或结论时参考。
- `DualValueMetricCard.tsx`：比较两个主体的数值和相对占比时参考。
- `ProgressStatusCard.tsx`：表达单个目标的进度、成熟度或完成状态时参考。

不要为了填满页面而堆卡片。长正文、复杂图表或超过两个主体的比较应使用其他结构。
