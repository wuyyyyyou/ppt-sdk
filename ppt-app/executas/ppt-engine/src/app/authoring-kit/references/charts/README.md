# Charts（图表参考）

- `BarChart.tsx`：比较少量类别或主体的数值大小时参考。
- `LineChart.tsx`：表达按时间或顺序变化的趋势时参考。
- `RadarChart.tsx`：比较少量主体的多个共同维度时参考。
- `DonutChart.tsx`：表达一个整体内部的少量占比时参考。

图表依赖父容器尺寸时必须配合 `foundations/MeasuredChartArea.tsx`。数据必须来自允许的事实来源；没有数据时不要复制 Preview 中的示意数值。
