# Reference Library（参考实现库）

Reference Implementations（参考实现）是供 Agent 阅读、复制、拆解和改写的只读示例，不是需要保持接口兼容的组件库。

参考实现会通过分层 README 按类别导航，例如 `cards/`、`charts/`、`timelines/` 和 `media/`。Page Source 不应直接导入这个目录中的文件，而应根据当前页面需要复制并改写相关做法。
