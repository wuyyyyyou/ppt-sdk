# Chart Analytics Canvas slides

`slides/*.tsx` are the registered entry files for this canvas template group. They re-export `../blueprints/*.tsx` so `group.json.layouts` and `manifest.json` can reference only `slides/`.

For a concrete deck, derive final pages here and replace canvas slot guides with component compositions from `../components/`.
