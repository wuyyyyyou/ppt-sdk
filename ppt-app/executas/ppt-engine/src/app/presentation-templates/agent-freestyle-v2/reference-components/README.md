# Reference Components（参考组件）

这里保留了 `red-finance-v3` 的组件源码，供 Agent 查询图片容错、图表尺寸、导出边界、矩阵、时间线和信息层级的实现方式。

这些文件不是蓝图，也不是默认设计系统。Agent 可以复制、拆解或改写其中的局部实现，但不应直接把高层卡片、面板和整页结构当成每页的固定构图。图表与 `ImageShowcasePanel` 尤其适合作为技术参考。
