# Agent Freestyle v2 页面创作说明

从空白画布直接创作最终页面。业务内容和结构写在 `slides/*.tsx`；页面只需默认导出 React 组件，不需要 Schema、布局元数据或对应的数据 JSON。

根据当前讨论直接进行内容叙事、视觉方向和页面级构思与实现。需要时可以创建过程文档，但不预设固定阶段或文件名。

优先保证每页有明确视觉焦点、信息层级和独特构图。可按需使用 `components/` 的技术组件，并从 `reference-components/` 借鉴实现；不要因已有组件存在而把页面退化成卡片列表或固定模板。
