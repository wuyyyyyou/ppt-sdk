{
  "name": "test-aps-host-upload-app",
  "private": true,
  "version": "0.1.0",
  "description": "Anna App for testing Host Upload backend upload and frontend download.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "anna-app dev",
    "validate": "anna-app validate --strict"
  },
  "devDependencies": {
    "@anna-ai/cli": "latest"
  }
}
